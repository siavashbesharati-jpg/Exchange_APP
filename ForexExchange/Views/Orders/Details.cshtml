@model ForexExchange.Models.Order

@{
    ViewData["Title"] = "جزئیات معامله";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>جزئیات معامله #@Model.Id</h2>
        <div>
            <a asp-action="Index" class="btn btn-secondary">بازگشت به لیست</a>
            <a asp-controller="Receipts" asp-action="Upload" asp-route-orderId="@Model.Id" class="btn btn-primary me-2">آپلود رسید برای این معامله</a>
            @if (Model.Status == ForexExchange.Models.OrderStatus.Open)
            {
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">ویرایش</a>
            }
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="بستن"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="بستن"></button>
        </div>
    }

    <div class="row g-3">
        <div class="col-md-6">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="mb-0">اطلاعات مشتری</h5>
                </div>
                <div class="card-body">
                    @if (Model.Customer != null)
                    {
                        <div class="mb-2"><strong>نام:</strong> @Model.Customer.FullName</div>
                        <div class="text-muted"><i class="fas fa-phone"></i> @Model.Customer.PhoneNumber</div>
                    }
                    else
                    {
                        <div class="text-muted">مشتری: @Model.CustomerId</div>
                    }
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="mb-0"> اطلاعات زمانی </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6"><strong>تاریخ ایجاد:</strong> @Model.CreatedAt.ToPersianDateTextify()
                        </div>
                        @if (Model.UpdatedAt.HasValue)
                        {
                            <div class="col-md-6"><strong>آخرین بروزرسانی:</strong>
                                @Model.UpdatedAt.Value.ToPersianDateTextify()</div>
                        }
                    </div>
                </div>detail
            </div>
        </div>
    </div>

    <div class="card mt-3">
        <div class="card-header">
            <h5 class="mb-0">جزئیات تبادل ارز</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-4"><strong>جفت ارز:</strong> @Model.FromCurrency?.Code / @Model.ToCurrency?.Code
                </div>
                <div class="col-md-4"><strong>مبلغ:</strong> @Model.Amount.ToString("N2") @Model.FromCurrency?.Code
                </div>
                <div class="col-md-4"><strong>نرخ تبادل:</strong> @Model.Rate.ToString("N4")</div>
            </div>
            <div class="mt-2"><strong>مقدار کل:</strong> @Model.TotalAmount.ToString("N2") @Model.ToCurrency?.Code</div>
        </div>
    </div>



    @if (Model.Transactions != null && Model.Transactions.Any())
    {
        <div class="card mt-3">
            <div class="card-header">
                <h5 class="mb-0">تراکنش‌های مرتبط</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>#</th>
                                <th>طرف معامله</th>
                                <th>مقدار</th>
                                <th>نرخ</th>
                                <th>وضعیت</th>
                                <th>تاریخ</th>
                                <th>جزئیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var transaction in Model.Transactions)
                            {
                                var isThisBuyOrder = transaction.BuyOrderId == Model.Id;
                                var counterpartOrder = isThisBuyOrder ? transaction.SellOrder : transaction.BuyOrder;
                                var counterpartCustomer = isThisBuyOrder ? transaction.SellerCustomer :
                                transaction.BuyerCustomer;
                                var tStatusClass = transaction.Status switch
                                {
                                    ForexExchange.Models.TransactionStatus.Pending => "badge bg-warning",
                                    ForexExchange.Models.TransactionStatus.PaymentUploaded => "badge bg-info",
                                    ForexExchange.Models.TransactionStatus.ReceiptConfirmed => "badge bg-primary",
                                    ForexExchange.Models.TransactionStatus.Completed => "badge bg-success",
                                    ForexExchange.Models.TransactionStatus.Failed => "badge bg-danger",
                                    _ => "badge bg-secondary"
                                };
                                var tStatusText = transaction.Status switch
                                {
                                    ForexExchange.Models.TransactionStatus.Pending => "در انتظار",
                                    ForexExchange.Models.TransactionStatus.PaymentUploaded => "رسید آپلود شده",
                                    ForexExchange.Models.TransactionStatus.ReceiptConfirmed => "رسید تأیید شد",
                                    ForexExchange.Models.TransactionStatus.Completed => "تکمیل شده",
                                    ForexExchange.Models.TransactionStatus.Failed => "ناموفق",
                                    _ => "نامشخص"
                                };
                                <tr>
                                    <td><span class="badge bg-secondary">@transaction.Id</span></td>
                                    <td>
                                        @if (counterpartCustomer != null)
                                        {
                                            <a asp-controller="Customers" asp-action="Details"
                                                asp-route-id="@counterpartCustomer.Id" class="text-decoration-none">
                                                @counterpartCustomer.FullName
                                            </a>
                                        }
                                        else
                                        {
                                            <span class="text-muted">نامشخص</span>
                                        }
                                    </td>
                                    <td>@transaction.Amount.ToString("N2")</td>
                                    <td class="text-primary fw-bold">@transaction.Rate.ToString("N4")</td>
                                    <td><span class="@tStatusClass">@tStatusText</span></td>
                                    <td>
                                        <div>@transaction.CreatedAt.ToPersianDateTextify()</div>
                                        <small class="text-muted">@transaction.CreatedAt.ToString("HH:mm")</small>
                                    </td>
                                    <td>
                                        @if (counterpartOrder != null)
                                        {
                                            <a asp-controller="Orders" asp-action="Details" asp-route-id="@counterpartOrder.Id"
                                                class="btn btn-sm btn-outline-primary">
                                                مشاهده
                                            </a>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    @if (Model.Receipts != null && Model.Receipts.Any())
    {
        <div class="card mt-3">
            <div class="card-header">
                <h5 class="mb-0">فیش‌های مرتبط</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>نام فایل</th>
                                <th>نوع</th>
                                <th>وضعیت</th>
                                <th>تاریخ</th>
                                <th>عملیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var receipt in Model.Receipts)
                            {
                                <tr>
                                    <td>@receipt.FileName</td>
                                    <td>
                                        <span
                                            class="badge @(receipt.Type == ForexExchange.Models.ReceiptType.PaymentReceipt ? "bg-primary" : "bg-info")">
                                            @(receipt.Type == ForexExchange.Models.ReceiptType.PaymentReceipt ? "رسید پرداخت" :
                                                                                "گردش حساب بانکی")
                                        </span>
                                    </td>
                                    <td>
                                        @if (receipt.IsVerified)
                                        {
                                            <span class="badge bg-success">تأیید شده</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning">در انتظار تأیید</span>
                                        }
                                    </td>
                                    <td>@receipt.UploadedAt.ToString("yyyy/MM/dd HH:mm")</td>
                                    <td>
                                        <a asp-controller="Receipts" asp-action="Details" asp-route-id="@receipt.Id"
                                            class="btn btn-sm btn-outline-secondary">جزئیات</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    <div class="card mt-3">
        <div class="card-body d-flex justify-content-between">
            <a asp-action="Index" class="btn btn-secondary">بازگشت</a>
            @if (Model.Status != ForexExchange.Models.OrderStatus.Cancelled && Model.Status !=
                        ForexExchange.Models.OrderStatus.Completed)
            {
                <form asp-action="Delete" asp-route-id="@Model.Id" method="post"
                    onsubmit="return confirm('آیا از لغو این معامله اطمینان دارید؟')">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger">لغو معامله</button>
                </form>
            }
        </div>
    </div>
</div>
