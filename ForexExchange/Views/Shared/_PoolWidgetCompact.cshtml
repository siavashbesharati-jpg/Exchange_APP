@model List<ForexExchange.Models.CurrencyPool>

<div class="card">
    <div class="card-header bg-primary text-white py-2">
        <div class="d-flex justify-content-between align-items-center">
            <h6 class="mb-0"><i class="fas fa-coins me-2"></i>صندوق ارزی</h6>
            <button class="btn btn-sm btn-outline-light" onclick="location.reload()">
                <i class="fas fa-sync-alt"></i>
            </button>
        </div>
    </div>
    <div class="card-body p-0">
        @if (Model != null && Model.Any())
        {
            <div style="max-height: 180px; overflow-y: auto;">
                <table class="table table-sm mb-0">
                    @foreach (var pool in Model)
                    {
                        var balanceClass = pool.Balance >= 0 ? "text-success" : "text-danger";
                        var balanceIcon = pool.Balance >= 0 ? "fas fa-arrow-up" : "fas fa-arrow-down";
                        
                        <tr>
                            <td class="align-middle" style="width: 50px;">
                                <span style="font-size: 1.2rem;">
                                    @{
                                        var flag = pool.Currency?.Code switch
                                        {
                                            "IRR" => "🇮🇷",
                                            "USD" => "🇺🇸", 
                                            "EUR" => "🇪🇺",
                                            "AED" => "🇦🇪",
                                            "OMR" => "🇴🇲",
                                            "TRY" => "🇹🇷",
                                            "CNY" => "🇨🇳",
                                            _ => "💱"
                                        };
                                    }
                                    @flag
                                </span>
                            </td>
                            <td class="align-middle">
                                <div>
                                    <strong style="font-size: 0.9rem;">@pool.Currency?.Code</strong>
                                    <br>
                                    <small class="text-muted">@pool.Currency?.PersianName</small>
                                </div>
                            </td>
                            <td class="align-middle text-end">
                                <div class="@balanceClass">
                                    <i class="@balanceIcon" style="font-size: 0.8rem;"></i>
                                    @{
                                        var balance = pool.CurrencyCode == "IRR" 
                                            ? pool.Balance.ToString("N0") 
                                            : pool.Balance.ToString("N2");
                                    }
                                    <strong style="font-size: 0.85rem;">@balance</strong>
                                </div>
                                @if (pool.ActiveBuyOrderCount > 0 || pool.ActiveSellOrderCount > 0)
                                {
                                    <div style="font-size: 0.7rem;">
                                        @if (pool.ActiveBuyOrderCount > 0)
                                        {
                                            <span class="badge bg-success" style="font-size: 0.6rem;">
                                                <i class="fas fa-plus"></i> @pool.ActiveBuyOrderCount
                                            </span>
                                        }
                                        @if (pool.ActiveSellOrderCount > 0)
                                        {
                                            <span class="badge bg-danger" style="font-size: 0.6rem;">
                                                <i class="fas fa-minus"></i> @pool.ActiveSellOrderCount
                                            </span>
                                        }
                                    </div>
                                }
                            </td>
                        </tr>
                    }
                </table>
            </div>
        }
        else
        {
            <div class="text-center text-muted py-4">
                <i class="fas fa-coins fa-2x mb-2 d-block"></i>
                <p class="mb-0">هیچ ارزی در صندوق موجود نیست</p>
            </div>
        }
    </div>
</div>
